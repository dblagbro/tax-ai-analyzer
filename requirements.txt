# Web framework
flask==3.0.3
flask-login==0.6.3
gunicorn==22.0.0

# LLM providers
anthropic==0.96.0
openai==2.32.0

# Google OAuth / Gmail / Drive
google-auth==2.29.0
google-auth-oauthlib==1.2.0
google-auth-httplib2==0.2.0
google-api-python-client==2.130.0

# Cloud storage
dropbox==11.36.2
boto3==1.34.110

# Plaid (bank data aggregator — tier-3 alternative to SimpleFIN)
plaid-python==39.1.0

# PDF generation (WeasyPrint)
weasyprint==63.1

# Database / data
jinja2==3.1.4
openpyxl==3.1.2

# HTTP
httpx==0.27.0
requests==2.32.3

# Scheduling
apscheduler==3.10.4

# Vector store (optional — catches ImportError if GPU missing)
chromadb==0.5.0

# Browser automation (bank importers) — patchright is a hardened Playwright
# drop-in that pre-patches CDP fingerprints (Runtime.Enable leak, etc.) at the
# driver level, replacing the need for playwright-stealth at the call site.
patchright>=1.48,<2

# Camoufox: hardened Firefox fork (Step 7 last-resort engine). Engaged when
# patchright+Chrome is detected by a bank's bot-protection. Selected per-bank
# via the `<slug>_browser_engine="firefox"` setting. Binary downloaded by the
# Dockerfile via `python -m camoufox fetch`.
camoufox[geoip]>=0.4,<1

# PDF parsing (Chime statement importer)
pdfplumber==0.11.9

# Utilities
python-dateutil==2.9.0
python-dotenv==1.0.1
pyyaml==6.0.2
aiofiles==23.2.1

# ── LLM-free ingestion paths (2026-09-06, 2023 tax-prep push) ────────────────
# Both work with ZERO LLM calls — they close data gaps even while the proxy
# key is being reissued.
#
# invoice2data: structured field extraction from PDF invoices/receipts via
# per-vendor YAML templates. Runs BEFORE the AI-review LLM call to short-
# circuit obvious invoice-shaped docs. Needs pdftotext (poppler-utils, already
# in the Dockerfile). https://github.com/invoice-x/invoice2data
invoice2data>=0.4,<1

# ofxstatement: parses bank/card statements the user downloaded manually as
# CSV/PDF and emits OFX, which our existing ofx_importer already ingests.
# Direct workaround for the Playwright scrapers that failed in April — the
# user downloads 2023 statements from the bank portal, drops them in consume/,
# and March–December fills in with no LLM and no scraper.
# https://github.com/kedder/ofxstatement  (plugins per bank on PyPI)
ofxstatement>=0.9,<1
ofxparse>=0.21,<1

# Test runner — kept in the image so `docker exec tax-ai-analyzer pytest`
# works after a fresh rebuild without a separate manual `pip install`.
pytest>=8,<10
